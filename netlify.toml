[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_ENV = "production"
  NEXT_PUBLIC_VERCEL_ENV = "production"

[[plugins]]
  package = "@netlify/plugin-nextjs"
